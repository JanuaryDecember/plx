# Aplikacja
spring.application.name=plx-podlasie

# Baza danych
spring.datasource.url=jdbc:postgresql://127.0.0.1:32868/postgres
pring.datasource.username=postgres
spring.datasource.password=1nd3v3l()pm3nT
spring.datasource.driver-class-name=org.postgresql.Driver

# Ustawienia HikariCP
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.maximum-pool-size=100
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.max-lifetime=1800000

# JPA
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.generate-ddl=true
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.hikari.maximumPoolSize=100
spring.jpa.properties.hibernate.hikari.minimumIdle=5
spring.jpa.properties.hibernate.hikari.idleTimeout=30000
spring.jpa.properties.hibernate.hikari.maxLifetime=1800000

# Inicjalizacja SQL
spring.sql.init.mode=always
spring.sql.init.platform=postgresql

# Ciasteczka sesji
server.servlet.session.cookie.same-site=none
server.servlet.session.cookie.max-age=1800
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.secure=true

# Konfiguracja e-mail
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=januaryexperimental@gmail.com
spring.mail.password={secret}

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000
